language: python

python:
  - "2.7"
  - "3.6"

git:
  quiet: true

branches:
  only:
  - master

before_script:
  - pip install twine wheel

script:
  - pip --no-cache-dir install -e .
  - aws-gen -h
  - python setup.py sdist bdist_wheel

